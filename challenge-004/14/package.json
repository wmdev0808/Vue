{
  "name": "13",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx serve & npx json-server --watch db.json --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "hook",
    "json-server",
    "promise"
  ],
  "author": "Paul",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.3"
  }
}
